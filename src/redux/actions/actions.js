export const setAppName = (appName)=>{
  return {
    type: 'SET_APPNAME',
    appName
  }
}

export const setLocation = (location)=>{
  return {
    type: 'SET_LOCATION',
    location
  }
}

export const setLogin = (login)=>{
  return {
    type: 'SET_LOGIN',
    login
  }
}